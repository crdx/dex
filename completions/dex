#!/bin/bash
# shellcheck disable=SC2207

# =============================================================================
#
#   THIS FILE IS NOT AUTOGENERATED!
#
#   It contains custom completions (e.g. file completions for paste/upload,
#   dynamic completions from `dex ls --refs`) that the generator does not
#   produce. Do not overwrite this file with generated output.
#
# =============================================================================

MIME_TYPES='
    application/gzip
    application/json
    application/octet-stream
    application/pdf
    application/xml
    application/zip
    image/gif
    image/jpeg
    image/png
    image/webp
    text/css
    text/csv
    text/html
    text/javascript
    text/markdown
    text/plain
    text/xml
    video/mp4
    video/ogg
    video/webm
'

# Check if a flag is already used in COMP_WORDS.
function _has_flag {
    local flag="$1"
    local word
    for word in "${COMP_WORDS[@]}"; do
        if [ "$word" = "$flag" ]; then
            return 0
        fi
    done
    return 1
}

# Filter out flags that are already used.
function _filter_used {
    local flags="$1"
    local result=""
    local flag
    for flag in $flags; do
        if ! _has_flag "$flag"; then
            result="$result $flag"
        fi
    done
    echo "$result"
}

function _cmd_dex {
    local cur
    cur="${COMP_WORDS[COMP_CWORD]}"

    if [ "$COMP_CWORD" -eq 1 ]; then
        COMPREPLY=($(compgen -W 'paste upload redir cat rm ls cp mv set expose urls deploys conf e tail gc' -- "$cur"))
    else
        case ${COMP_WORDS[1]} in
            paste) _cmd_dex_paste ;;
            upload) _cmd_dex_upload ;;
            redir) _cmd_dex_redir ;;
            cat) _cmd_dex_cat ;;
            rm) _cmd_dex_rm ;;
            ls) _cmd_dex_ls ;;
            cp) _cmd_dex_cp ;;
            mv) _cmd_dex_mv ;;
            set) _cmd_dex_set ;;
            expose) _cmd_dex_expose ;;
            urls) _cmd_dex_urls ;;
            deploys) _cmd_dex_deploys ;;
            conf) _cmd_dex_conf ;;
            e) _cmd_dex_e ;;
            tail) _cmd_dex_tail ;;
            gc) _cmd_dex_gc ;;
        esac
    fi
}

function _cmd_dex_paste {
    local cur prev
    cur="${COMP_WORDS[COMP_CWORD]}"
    prev="${COMP_WORDS[COMP_CWORD-1]}"

    if [ "$prev" = "--label" ] || [ "$prev" = "-l" ]; then
        compopt +o default +o bashdefault
        COMPREPLY=()
    elif [ "$prev" = "--type" ] || [ "$prev" = "-t" ]; then
        compopt +o filenames
        COMPREPLY=($(compgen -W "$MIME_TYPES" -- "$cur"))
    elif [ "$COMP_CWORD" -ge 2 ]; then
        COMPREPLY=($(compgen -W "$(_filter_used '--label --uuid --force --type --bare')" -- "$cur"))
        local IFS=$'\n' # Handle filenames with spaces.
        COMPREPLY+=($(compgen -f -- "$cur"))
    fi
}

function _cmd_dex_upload {
    local cur prev
    cur="${COMP_WORDS[COMP_CWORD]}"
    prev="${COMP_WORDS[COMP_CWORD-1]}"

    if [ "$prev" = "--label" ] || [ "$prev" = "-l" ]; then
        compopt +o default +o bashdefault
        COMPREPLY=()
    elif [ "$prev" = "--type" ] || [ "$prev" = "-t" ]; then
        compopt +o filenames
        COMPREPLY=($(compgen -W "$MIME_TYPES" -- "$cur"))
    elif [ "$COMP_CWORD" -ge 2 ]; then
        COMPREPLY=($(compgen -W "$(_filter_used '--label --uuid --force --type --bare')" -- "$cur"))
        local IFS=$'\n' # Handle filenames with spaces.
        COMPREPLY+=($(compgen -f -- "$cur"))
    fi
}

function _cmd_dex_redir {
    local cur prev
    cur="${COMP_WORDS[COMP_CWORD]}"
    prev="${COMP_WORDS[COMP_CWORD-1]}"

    if [ "$COMP_CWORD" -eq 2 ]; then
        compopt +o filenames
        COMPREPLY=($(compgen -W 'http:// https://' -- "$cur"))
    elif [ "$prev" = "--label" ] || [ "$prev" = "-l" ]; then
        compopt +o default +o bashdefault
        COMPREPLY=()
    elif [ "$COMP_CWORD" -ge 3 ]; then
        COMPREPLY=($(compgen -W "$(_filter_used '--label --force')" -- "$cur"))
    fi
}

function _cmd_dex_cat {
    local cur
    cur="${COMP_WORDS[COMP_CWORD]}"

    if [ "$COMP_CWORD" -ge 2 ]; then
        COMPREPLY=($(compgen -W "$(dex ls --refs) $(_filter_used '--force')" -- "$cur"))
    fi
}

function _cmd_dex_rm {
    local cur
    cur="${COMP_WORDS[COMP_CWORD]}"

    if [ "$COMP_CWORD" -ge 2 ]; then
        COMPREPLY=($(compgen -W "$(dex ls --refs)" -- "$cur"))
    fi
}

function _cmd_dex_ls {
    local cur flags
    cur="${COMP_WORDS[COMP_CWORD]}"

    if [ "$COMP_CWORD" -ge 2 ]; then
        if _has_flag "--refs"; then
            COMPREPLY=()
        else
            COMPREPLY=($(compgen -W "$(_filter_used '--refs --verbose')" -- "$cur"))
        fi
    fi
}

function _cmd_dex_cp {
    local cur
    cur="${COMP_WORDS[COMP_CWORD]}"

    if [ "$COMP_CWORD" -ge 2 ] && [ "$COMP_CWORD" -le 3 ]; then
        COMPREPLY=($(compgen -W "$(dex ls --refs)" -- "$cur"))
    fi
}

function _cmd_dex_mv {
    local cur
    cur="${COMP_WORDS[COMP_CWORD]}"

    if [ "$COMP_CWORD" -ge 2 ] && [ "$COMP_CWORD" -le 3 ]; then
        COMPREPLY=($(compgen -W "$(dex ls --refs)" -- "$cur"))
    fi
}

function _cmd_dex_set {
    local cur
    cur="${COMP_WORDS[COMP_CWORD]}"

    if [ "$COMP_CWORD" -eq 2 ]; then
        COMPREPLY=($(compgen -W "$(dex ls --refs)" -- "$cur"))
    elif [ "$COMP_CWORD" -eq 3 ]; then
        COMPREPLY=($(compgen -W "type kind" -- "$cur"))
    elif [ "$COMP_CWORD" -eq 4 ]; then
        case ${COMP_WORDS[3]} in
            kind) COMPREPLY=($(compgen -W 'file paste redir' -- "$cur")) ;;
            type) compopt +o filenames; COMPREPLY=($(compgen -W "$MIME_TYPES" -- "$cur")) ;;
        esac
    fi
}

function _cmd_dex_expose {
    local cur prev
    cur="${COMP_WORDS[COMP_CWORD]}"
    prev="${COMP_WORDS[COMP_CWORD-1]}"

    if [ "$COMP_CWORD" -eq 2 ]; then
        COMPREPLY=($(compgen -W "$(dex ls --refs)" -- "$cur"))
    elif [ "$prev" = "--expiry" ] || [ "$prev" = "-x" ]; then
        COMPREPLY=($(compgen -W '15m 30m 1h 6h 12h 1d 7d 30d' -- "$cur"))
    elif [ "$COMP_CWORD" -ge 3 ]; then
        COMPREPLY=($(compgen -W "$(_filter_used '--expiry')" -- "$cur"))
    fi
}

function _cmd_dex_urls {
    local cur
    cur="${COMP_WORDS[COMP_CWORD]}"

    if [ "$COMP_CWORD" -eq 2 ]; then
        COMPREPLY=($(compgen -W 'rm' -- "$cur"))
    elif [ "$COMP_CWORD" -ge 3 ]; then
        compopt +o default +o bashdefault
        COMPREPLY=()
    fi
}

function _cmd_dex_deploys {
    local cur
    cur="${COMP_WORDS[COMP_CWORD]}"

    if [ "$COMP_CWORD" -ge 2 ]; then
        COMPREPLY=($(compgen -W "$(_filter_used '--all --verbose')" -- "$cur"))
    fi
}

function _cmd_dex_conf {
    local cur
    cur="${COMP_WORDS[COMP_CWORD]}"

    if [ "$COMP_CWORD" -ge 2 ]; then
        COMPREPLY=($(compgen -W "$(_filter_used '--edit')" -- "$cur"))
    fi
}

function _cmd_dex_e {
    compopt +o default +o bashdefault
    COMPREPLY=()
}

function _cmd_dex_tail {
    local cur
    cur="${COMP_WORDS[COMP_CWORD]}"

    if [ "$COMP_CWORD" -ge 2 ]; then
        COMPREPLY=($(compgen -W "$(_filter_used '--verbose')" -- "$cur"))
    fi
}

function _cmd_dex_gc {
    compopt +o default +o bashdefault
    COMPREPLY=()
}

complete -o bashdefault -o default -o filenames -F _cmd_dex dex
